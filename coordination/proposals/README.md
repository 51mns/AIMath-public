# Proposals

AI and human contributors may propose Campaigns, Tasks or Portfolio changes here. Proposals are non-canonical until a human maintainer accepts them into the canonical Campaign/Task/Portfolio source files.

Subdirectories:

- `campaigns/`
- `tasks/`
- `portfolio/`

Default quotas: one active campaign proposal and three active task proposals per actor.
