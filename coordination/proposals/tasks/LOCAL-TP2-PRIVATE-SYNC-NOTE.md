# Local TP2 private/public state-sync note

Status: PROPOSAL / NON-CANONICAL RESEARCH AUTHORITY

This note records a private/public synchronization gap for Local TP2 without importing private branch state as public mathematical truth.

## Public canonical state

Public Village canonical authority remains current public `main` plus merged public research/Outcome/Evaluation/Task records. Open PRs, private branches, chat summaries, and external webpages do not establish a canonical Outcome.

At the time this proposal branch was created, public `main` was:

`a1b0243423bbc6da08e514e37597feb987163e2d`

The existing public worker PR #73 for `TASK-LOCAL-TP2-REFRESH-001` reports an Outcome candidate `STRUCTURAL_REDUCTION`, but until merged it is not canonical source authority for post-outcome Director processing.

## Private lead to preserve as a synchronization gap

The private repository `51mns/AIMath` contains later Local TP2 research branches that are ahead of the public snapshot. They include a continuant/network feasibility result and later mirror-factor/weighted-skein work. These private branch results are useful routing context only until they are independently exported or reconstructed through the public repository process.

This proposal deliberately does **not**:

- copy private branch artifacts into public canonical research;
- create a public Outcome from unmerged/private evidence;
- promote `C-LOCAL-TP2`;
- establish publication novelty;
- approve a follow-up Task;
- create or alter a lock;
- reopen/activate a Campaign by itself.

## Required public path before autonomous `/next` continuation

To make `/join` and `/next` continue Local TP2 from the newer architecture without silently jumping to unrelated work, the public Village needs the following sequence:

1. finish and merge an admissible public research contribution for the current Local TP2 source Task;
2. create/freeze its canonical Outcome and any due Portfolio Evaluation through the current post-outcome Director policy;
3. if continuation is recommended, create a bounded follow-up Task in `PROPOSED` state;
4. obtain the required human governance decision and promote that Task to `APPROVED`;
5. only then may ordinary `/join` or `/next` select the follow-up as a same-Campaign READY Task, subject to dependency/collision/capacity/capability gates.

If no eligible same-Campaign APPROVED follow-up exists, `/next` is allowed to rank eligible READY work in other Campaigns.

## Suggested follow-up objective for human review

A plausible bounded follow-up, if/when supported by public canonical source evidence, is to test whether the Local TP2 structural reduction can be closed by an exact graded switching / weighted-skein correction mechanism rather than by generic coefficientwise TP2 transfer.

This is intentionally a routing suggestion, not a canonical Task definition or mathematical claim.
