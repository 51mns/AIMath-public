# Public research packages

Only claim packages that have passed the public-export privacy/provenance gate belong here.

A package should be understandable without access to private AIMath conversations or branches.

Use `templates/claim-package/` for new exports.
