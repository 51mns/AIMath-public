# Local TP2 novelty audit

The exact theorem candidate is unproved. Search failure is never treated as proof of novelty.

## Known background/components

The frozen source audit treats the following as known background:

- mirror/deformed-squared Markov mutation and Farey-tree structure;
- positive symmetric Laurent entries in the tree;
- Farey / continued-fraction structure;
- the symmetric Laurent / Chebyshev coefficient-profile convention `H`;
- generic TP2 / monotone-likelihood-ratio cross-product vocabulary.

These ingredients do not by themselves imply the frozen determinant inequality for the derived rows `H(U-C)` and `H(V-U)`.

## Near-known neighbourhood

The audit also compared against Cohn/continuant/convergent determinant structures and Pólya-frequency / PF/PF2 structures. The checked results concern different matrices, indices or single-sequence properties; no formula-level equivalence to the frozen adjacent coefficient-profile cross-minor was established.

## Restricted exact-match label

For the exact frozen statement

```text
F_v(n) > 0 for 0 <= n <= deg_x(S_v),
```

with its reviewed derived rows, coefficient-index direction, terminal endpoint, zero extension and degree orientation, no exact theorem-level match was identified in the documented audit scope.

Restricted label:

```text
POTENTIALLY_NEW
```

This means only “no exact match identified in the documented bounded audit”. It does **not** establish publication novelty, historical priority or even a proved theorem.

## Publication status

```text
NOVELTY_NOT_ESTABLISHED
```

No `NEW`, `FIRST`, `NOVEL`, world-first or priority wording is licensed by this package.

**Provenance:** writer/source audit `9507c4be2a859a284683d62672b14e35f5681549`; independent source/definition review `5380e3f898eb0a91d7f47b48a50368637af218e0`.
